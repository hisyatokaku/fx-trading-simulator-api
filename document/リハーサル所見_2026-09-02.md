# リハーサル所見と対応方針（2026-09-02）

リハーサルで見つかった 18 項目の整理。詳細な調査ログは `document/tmp/rehearsal_findings_memo.md`。
関連: PR #25（Day 2 修正一式）、運営手順.md §5-5（app サーバ負荷・スケールアップ、対応済み）。

**優先度**: 🔴 当日の成立・公平性を壊す ／ 🟡 混乱・UX の問題 ／ ⚪ 品質 ／ 🔵 設計判断が必要

---

## 🔴 高優先（イベント前に必ず対応）

| # | 件名 | 対象 | 対応 |
|---|---|---|---|
| 8 | **API・repo から本番（EVAL）レートが事前に見える**。`GET /api/scenario/EVAL5/rates` で当日 1,440 分を一括取得できることを外部から実証。public repo の `data/rates.csv` にも同データ。さらに変更系 API（scenario 作成/削除、rate/bulk 等）が全て認証なし | サーバー（app/api）+ `data/rates.csv` | **A. 秘密シナリオ方式**（repo に入れないデータを評価直前に DB 投入、EVAL0〜11 は練習用に降格）を軸に。競技性を重視するなら B. EVAL 期間のレート参照 API を拒否、C. 変更系 API に管理トークン、を追加 |
| 11 | 評価シナリオ名は**事前でなく開始後しばらく経ってから発表**する形式にする | 運用ルール（教材変更なし） | 発表タイミング（例: 終了 60 分前）と発表方法を決める。#8 の A 案と組み合わせると「発表前に答えを見る」余地がゼロになる。colab_template の文言は現状のままで整合 |
| 5 | **HTTP のため Chrome で初回ログインに支障** → HTTPS 化したい | TLJH 設定（対応済み） | ✅ **対応済み（2026-09-02）**: sslip.io + Let's Encrypt で **https://35-243-84-187.sslip.io** を有効化（tcp:443 開放済み、証明書期限 2026-12-01・自動更新。実行ログ: `infra-setup/output/https_enable_20260902-2235.log`）。ドキュメントの URL 差し替えは `https-url-rewrite` ブランチで対応 |
| 3 | **tutorial.ipynb の USER_ID デフォルトが `testuser`**。許可リスト入りのためそのまま動いてしまい採点が汚れる | `tutorial.ipynb` | colab_template と同じ「デフォルトのままなら raise」チェックを追加 |
| 6 | 参加者向けガイド.ipynb に `BASE_URL = 'http://localhost:8000'`（ローカル API 前提の名残） | `参加者向けガイド.ipynb` | 環境変数 `FX_API_BASE_URL` 方式に書き換え（tutorial は対応済み） |

## 🟡 中優先（混乱防止・UX）

| # | 件名 | 対象 | 対応 |
|---|---|---|---|
| 1 | パスワードを忘れた人への対応 | TLJH / 運営手順.md | 推奨: `/opt/tljh/state/passwords.dbm` から該当キーだけ削除（ユーザーを消さず再設定させられる）。ワンライナーを運営手順に追記。フォールバックは既存のユーザー削除→再登録 |
| 2 | 初回ログインが「Sign in」表示で PW 新規設定と伝わらない | TLJH 設定 + `参加者向けガイド.ipynb` | 画面の Sign up 化は重い。**`announcement_login`（ログイン画面の告知文）**に「ここで決めた PW（7 文字以上）がそのまま登録されます」を表示（設定 1 ファイル + reload）。ガイドにも二重化 |
| 13 | 戦略提出時に「謎の URL」が出る | `tutorial.ipynb`（answer も同期） | 調査済み: 成功時に URL を出すコードは無く、正体は**失敗時の requests traceback**（`... for url: http://34.146...` を含む）。改善: HTTPError を捕捉し `detail` を短い日本語メッセージに変換。特に 403 は「配布された ID を設定してください」と案内 |
| 15 | 最初の `proceed_to_end()` で固まったと誤解される | `colab_template.ipynb` | 「1 シナリオ約 20 秒かかります」のコメントを実行例セルに追加（50 人同時ではさらに伸びる → 運営手順 §5-5 のスケールアップで緩和） |
| 18 | tutorial.ipynb の「**DB に登録済みの ID** しか使えません」（2 箇所） | `tutorial.ipynb`（2 箇所） | 挙動としては正しくなったが実装詳細を見せない表現に置換: 「当日配布された ID を USER_ID に設定。それ以外はエラー（403）」 |
| 17 | 「取引開始日/終了日が提供されます」が 1 日制と噛み合わない（colab_template） | `colab_template.ipynb` | 「対象日（1 日分・1,440 ステップ）」という表現に変更 |
| 4 | AI 利用可の明文化 | `tutorial.ipynb`・`colab_template.ipynb`・ガイド | tutorial / colab_template のルール節 + 参加者向けガイドに「生成 AI・検索の利用は自由」を追加 |
| 9 | 日本語フォント導入に kernel 再起動が必要そう | TLJH 環境（04 スクリプト）+ 教材冒頭 | 原因は matplotlib のフォントキャッシュ（既知挙動）。根本対策: `fonts-noto-cjk` + `japanize-matplotlib` を TLJH に事前インストール（04 スクリプトに追加 + 既存 VM へ手動適用）→ 参加者側の作業ゼロに当面の回避策（リハーサルで確認済み）: ノートブック上で `pip install japanize-matplotlib` → `import japanize_matplotlib` の 2 行。事前導入が間に合わない場合は教材にこの 2 行を書いておく |

## ⚪ 低優先（品質）

| # | 件名 | 対象 | 対応 |
|---|---|---|---|
| 10 | tutorial のコード内コメントが英語混じり | `tutorial.ipynb`・`tutorial_answer.ipynb` | 日本語に統一（tutorial.ipynb と tutorial_answer.ipynb を同期して修正） |
| 14 | colab_template の `# evaluationation:` タイポ | `colab_template.ipynb` | `# evaluation:` に修正 |
| 16 | 未使用関数の整理 | `colab_template.ipynb` | 実測の結果、`generate_last_n_business_days` は**現役**（履歴プリフェッチで使用）。真の未使用は **`plot_chart`**（GoldenCross 削除の副産物）。削除するか、x 軸問題を直した上で「提供関数」として案内するかの二択 |

## 🔵 設計判断が必要（決めてから着手）

| # | 件名 | 対象 | 論点 |
|---|---|---|---|
| 7 | 初期資産 1,000,000 → 1 億円 | `data/scenarios.csv` + 教材全般 | 技術的障害なし（DB は numeric(20,6) で桁余裕、scenarios.csv + seed で反映）。注意: ①「100 万円」記載が教材複数箇所にあり一括変更必須、②開始済みセッションには効かない → イベント前に変更、③チュートリアル合格ライン（scenario_review）の再計算 |
| 12 | TEST シナリオが 2026-06 の 1 週間に集中しすぎ（TEST4 は TEST2 と同日） | `data/` + `colab_template.ipynb` | 年・相場局面をバラして選び直す。EVAL の 11 日と被らない日を選ぶこと。1 日あたり rates 約 1.9 万行のデータ調達が必要。TEST4 の重複は意図の確認を |

---

## 追加所見（2026-09-02 追記・第 2 便）

| # | 件名 | 対象 | 対応 |
|---|---|---|---|
| 19 | FAQ の新設（AI 利用の説明を含む） | `tutorial.ipynb`・ガイド | tutorial / ガイドに FAQ 節を追加。#4 の文言もここに統合 |
| 20 | Day 2 教材の「day」表現が日足前提のまま | `colab_template.ipynb` | "day" → "tick" に統一（proceed_one_day の説明等） |
| 21 | 実行時の不要な出力 2 つ | ① `tutorial.ipynb` ／ ② TLJH 環境 | ① start URL が毎回出力される（コードに print(url) は無し。出どころ未特定（実行時のみ再現））→ 削除 ② `findfont: ... weight bold` 警告 → #9 のフォント事前導入とセットで解消 |
| 22 | GoldenCross 戦略例 markdown の再考 | `colab_template.ipynb` | Day 1 で移動平均を説明するなら削除。「——」等の文体は使わない |
| 23 | colab_template Cell 0 の文言 5 点 | `colab_template.ipynb`（Cell 0） | 「その額を評価」／「以下のテンプレートコードは」／「最終日にその時のレートで」／英語併記（Scenario name 等）削除／「本番用（一回だけ）:」に |
| 24 | proceed_to_end に進捗表示（#15 と統合） | `colab_template.ipynb` | tqdm、難しければ 100 tick ごとに print。「走っている」ことを可視化 |
| 25 | 「上から順に実行」の範囲が不明瞭 | `colab_template.ipynb`（Cell 0 §5） | 事前準備セルの範囲を見出し・番号で明示 |

## 対応済み（参考）

- **app サーバ負荷**: 1 人の全力実行で 1 コアの 75% を消費（e2-medium では同時 2〜3 人が限界）。当日スケールアップ手順を運営手順 §5-5 に記載済み
- **Day 2 教材（colab_template）の動かない問題一式**: PR #25 で修正済み（マージ待ち）
- **参加者 ID**: 再生成・Hub / traders 登録・旧 ID 削除まで完了
