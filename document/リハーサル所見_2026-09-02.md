# リハーサル所見と対応方針（2026-09-02）

リハーサルで見つかった 18 項目の整理。詳細な調査ログは `document/tmp/rehearsal_findings_memo.md`。
関連: PR #25（Day 2 修正一式）、運営手順.md §5-5（app サーバ負荷・スケールアップ、対応済み）。

**優先度**: 🔴 当日の成立・公平性を壊す ／ 🟡 混乱・UX の問題 ／ ⚪ 品質 ／ 🔵 設計判断が必要

**進捗（2026-09-03 時点）**: 26 項目中 — ✅ 対応済み 7（#5 HTTPS、#14/#16/#17/#20/#23/#24 は PR #28 で対応済みと判明）
／ 🔶 適用済み・レビュー中 6（#3/#6/#15/#18/#25/#26 → PR #32）＋ 一部対応 3（**#8 は PR #31/#33 で API 側を閉鎖済み**・#12/#22）／ 残りは未着手（詳細は各行の対応欄）

---

## 🔴 高優先（イベント前に必ず対応）

| # | 件名 | 対象 | 対応 |
|---|---|---|---|
| 8 | **API・repo から本番（EVAL）レートが事前に見える**。`GET /api/scenario/EVAL5/rates` で当日 1,440 分を一括取得できることを外部から実証。public repo の `data/rates.csv` にも同データ。さらに変更系 API（scenario 作成/削除、rate/bulk 等）が全て認証なし | サーバー（app/api）+ `data/rates.csv` | 🔶 **一部対応（PR #31/#33 マージ・本番で 403 を実測確認済み）**: 一覧からの EVAL 非表示（#31）と、`/{name}/rates`・`/api/rate/{ts}` の EVAL 期間 403 化（#33）はデプロイ済み。残りは **A. 秘密シナリオ方式**（repo に入れないデータを評価直前に DB 投入。public repo の rates.csv に答えが残る問題の本命）と **C. 変更系 API の管理トークン** |
| 11 | 評価シナリオ名は**事前でなく開始後しばらく経ってから発表**する形式にする | 運用ルール（教材変更なし） | 発表タイミング（例: 終了 60 分前）と発表方法を決める。#8 の A 案と組み合わせると「発表前に答えを見る」余地がゼロになる。colab_template の文言は現状のままで整合 |
| 5 | **HTTP のため Chrome で初回ログインに支障** → HTTPS 化したい | TLJH 設定（対応済み） | ✅ **対応済み（2026-09-02）**: sslip.io + Let's Encrypt で **https://35-243-84-187.sslip.io** を有効化（tcp:443 開放済み、証明書期限 2026-12-01・自動更新。実行ログ: `infra-setup/output/https_enable_20260902-2235.log`）。ドキュメントの URL 差し替えも **PR #30 でマージ済み** |
| 3 | **tutorial.ipynb の USER_ID デフォルトが `testuser`**。許可リスト入りのためそのまま動いてしまい採点が汚れる | `tutorial.ipynb` | 🔶 **適用済み・PR #32 レビュー中**: colab_template と同じ「デフォルトのままなら raise」チェックを追加 |
| 6 | 参加者向けガイド.ipynb に `BASE_URL = 'http://localhost:8000'`（ローカル API 前提の名残） | `参加者向けガイド.ipynb` | 🔶 **適用済み・PR #32 レビュー中**: 環境変数 `FX_API_BASE_URL` 方式に書き換え（tutorial は対応済み） |

## 🟡 中優先（混乱防止・UX）

| # | 件名 | 対象 | 対応 |
|---|---|---|---|
| 1 | パスワードを忘れた人への対応 | TLJH / 運営手順.md | 推奨: `/opt/tljh/state/passwords.dbm` から該当キーだけ削除（ユーザーを消さず再設定させられる）。ワンライナーを運営手順に追記。フォールバックは既存のユーザー削除→再登録 |
| 2 | 初回ログインが「Sign in」表示で PW 新規設定と伝わらない | TLJH 設定 + `参加者向けガイド.ipynb` | 画面の Sign up 化は重い。**`announcement_login`（ログイン画面の告知文）**に「ここで決めた PW（7 文字以上）がそのまま登録されます」を表示（設定 1 ファイル + reload）。ガイドにも二重化 |
| 13 | 戦略提出時に「謎の URL」が出る | `tutorial.ipynb`（answer も同期） | 調査済み: 成功時に URL を出すコードは無く、正体は**失敗時の requests traceback**（`... for url: http://34.146...` を含む）。改善: HTTPError を捕捉し `detail` を短い日本語メッセージに変換。特に 403 は「配布された ID を設定してください」と案内 |
| 15 | 最初の `proceed_to_end()` で固まったと誤解される | `colab_template.ipynb` | 🔶 **適用済み・PR #32 レビュー中**: 「1 シナリオ約 20 秒かかります」のコメントを実行例セルに追加（50 人同時ではさらに伸びる → 運営手順 §5-5 のスケールアップで緩和） |
| 18 | tutorial.ipynb の「**DB に登録済みの ID** しか使えません」（2 箇所） | `tutorial.ipynb`（2 箇所） | 🔶 **適用済み・PR #32 レビュー中**: 挙動としては正しくなったが実装詳細を見せない表現に置換: 「当日配布された ID を USER_ID に設定。それ以外はエラー（403）」 |
| 17 | 「取引開始日/終了日が提供されます」が 1 日制と噛み合わない（colab_template） | `colab_template.ipynb` | ✅ **対応済み（PR #28）**: 「対象日（1 日分・1,440 ステップ）」という表現に変更 |
| 4 | AI 利用可の明文化 | `tutorial.ipynb`・`colab_template.ipynb`・ガイド | tutorial / colab_template のルール節 + 参加者向けガイドに「生成 AI・検索の利用は自由」を追加 |
| 9 | 日本語フォント導入に kernel 再起動が必要そう | TLJH 環境（04 スクリプト）+ 教材冒頭 | 原因は matplotlib のフォントキャッシュ（既知挙動）。根本対策: `fonts-noto-cjk` + `japanize-matplotlib` を TLJH に事前インストール（04 スクリプトに追加 + 既存 VM へ手動適用）→ 参加者側の作業ゼロに当面の回避策（リハーサルで確認済み）: ノートブック上で `pip install japanize-matplotlib` → `import japanize_matplotlib` の 2 行。事前導入が間に合わない場合は教材にこの 2 行を書いておく |

## ⚪ 低優先（品質）

| # | 件名 | 対象 | 対応 |
|---|---|---|---|
| 10 | tutorial のコード内コメントが英語混じり | `tutorial.ipynb`・`tutorial_answer.ipynb` | 日本語に統一（tutorial.ipynb と tutorial_answer.ipynb を同期して修正） |
| 14 | colab_template の `# evaluationation:` タイポ | `colab_template.ipynb` | ✅ **対応済み（PR #28）**: `# evaluation:` に修正 |
| 16 | 未使用関数の整理 | `colab_template.ipynb` | ✅ **対応済み（PR #28）**: 実測の結果、`generate_last_n_business_days` は**現役**（履歴プリフェッチで使用）。真の未使用は **`plot_chart`**（GoldenCross 削除の副産物）。削除するか、x 軸問題を直した上で「提供関数」として案内するかの二択 |

## 🔵 設計判断が必要（決めてから着手）

| # | 件名 | 対象 | 論点 |
|---|---|---|---|
| 7 | 初期資産 1,000,000 → 1 億円 | `data/scenarios.csv` + 教材全般 | 技術的障害なし（DB は numeric(20,6) で桁余裕、scenarios.csv + seed で反映）。注意: ①「100 万円」記載が教材複数箇所にあり一括変更必須、②開始済みセッションには効かない → イベント前に変更、③チュートリアル合格ライン（scenario_review）の再計算 |
| 12 | TEST シナリオが 2026-06 の 1 週間に集中しすぎ（TEST4 は TEST2 と同日） | `data/` + `colab_template.ipynb` | 🔶 **一部対応済み（PR #28 で TEST4 重複を解消）**: 年・相場局面をバラして選び直す。EVAL の 11 日と被らない日を選ぶこと。1 日あたり rates 約 1.9 万行のデータ調達が必要。TEST4 の重複は意図の確認を |

---

## 追加所見（2026-09-02 追記・第 2 便）

| # | 件名 | 対象 | 対応 |
|---|---|---|---|
| 19 | FAQ の新設（AI 利用の説明を含む） | `tutorial.ipynb`・ガイド | **tutorial.ipynb には既に FAQ（AI 可・ローカル実行可）が存在**（cell 36）。残作業はガイドへの展開と FAQ の拡充 |
| 20 | Day 2 教材の「day」表現が日足前提のまま | `colab_template.ipynb` | ✅ **対応済み（PR #28）**: "day" → "tick" に統一（proceed_one_day の説明等） |
| 21 | 実行時の不要な出力 2 つ | ① `tutorial.ipynb` ／ ② TLJH 環境 | ① start URL が毎回出力される（コードに print(url) は無し。出どころ未特定（実行時のみ再現））→ 削除 ② `findfont: ... weight bold` 警告 → #9 のフォント事前導入とセットで解消 |
| 22 | GoldenCross 戦略例 markdown の再考 | `colab_template.ipynb` | 🔶 **一部対応済み（PR #28 で「——」を除去。削除判断は未決）**: Day 1 で移動平均を説明するなら削除。「——」等の文体は使わない |
| 23 | colab_template Cell 0 の文言 5 点 | `colab_template.ipynb`（Cell 0） | ✅ **対応済み（PR #28）**: 「その額を評価」／「以下のテンプレートコードは」／「最終日にその時のレートで」／英語併記（Scenario name 等）削除／「本番用（一回だけ）:」に |
| 24 | proceed_to_end に進捗表示（#15 と統合） | `colab_template.ipynb` | ✅ **対応済み（PR #28）**: tqdm、難しければ 100 tick ごとに print。「走っている」ことを可視化 |
| 25 | 「上から順に実行」の範囲が不明瞭 | `colab_template.ipynb`（Cell 0 §5） | 🔶 **適用済み・PR #32 レビュー中**: 事前準備セルの範囲を見出し・番号で明示 |
| 26 | tutorial_answer にローカル API 前提の残骸 2 箇所 | `tutorial_answer.ipynb`（cell 3, 36） | 🔶 **適用済み・PR #32 レビュー中**: ① cell 3 の「127.0.0.1:8000 で起動した場合」コメント削除 ② cell 36 講師メモの「2026-07-30 時点で応答なし・Dev Container ローカル起動前提」は**現状と矛盾する古い記述** → 削除。※配布版 tutorial.ipynb は環境変数方式で問題なし。なお answer 版 cell 3 は japanize_matplotlib / tqdm 導入済み＝ #9・#24 は配布版への同期で解決できる見込み |

## 適用修正の before/after 詳細（PR #32・レビュー用）

行番号は PR #32 の jupytext 抽出 .py 上の位置（= レビュー対象コミットの行）。ipynb へは逆変換で同期する。

### #3 USER_ID デフォルトを dummyUser に（3 ファイル）

`document/tutorial.py:64`（tutorial.ipynb cell 3）／ `tutorial_answer.py:65` ／ `参加者向けガイド.py:202`

```diff
- USER_ID  = os.environ.get("FX_USER_ID", "testuser")
+ USER_ID  = os.environ.get("FX_USER_ID", "dummyUser")  # 配布された ID に置き換えること（このままではエラーになります）
```
```diff
- USER_ID    = 'testuser'    # 配布されたユーザーID
+ USER_ID    = 'dummyUser'   # 配布されたユーザーID に置き換えること（このままではエラーになります）
```

### #6 ガイドの localhost を env 方式に

`document/参加者向けガイド.py:197-200`

```diff
- # BASE_URL は主催者から案内されたサーバーURLに変更してください
- BASE_URL   = 'http://localhost:8000'
+ # BASE_URL は演習環境（JupyterHub）では自動設定されます。他環境では主催者の案内に従ってください
+ import os
+ BASE_URL   = os.environ.get("FX_API_BASE_URL", "http://34.146.231.219:8000")
```

### #18 「DB に登録済み」記述の置換

`document/tutorial.py:194` ／ `tutorial_answer.py:195`

```diff
- # 4. `user_id` は **DB に登録済みのもの**しか使えません（未登録だとエラー）。
+ # 4. `user_id` は**当日配布された ID** を使ってください。それ以外の ID はエラー（403）になります。
```

`document/tutorial_answer.py:257`

```diff
- #   - userId が DB 未登録だとサーバ側で例外になる（初期登録済みなのは testuser など）
+ #   - userId が未登録だと 403 エラーになる（配布された ID を使うこと）
```

### #26 answer のローカル API 残骸（3 箇所）

`document/tutorial_answer.py:63`（行削除）

```diff
- # ローカルで API を起動した場合は 127.0.0.1:8000（README の Quick Start 参照）。
```

`document/tutorial_answer.py:1013-1014`（講師用メモ・運用上の注意）

```diff
- # - **API サーバ**: 共有サーバ `http://os3-389-27987.vs.sakura.ne.jp` は接続は受け付けるものの
- #   応答が返らない状態を確認しています（2026-07-30 時点）。**Dev Container でのローカル起動を前提**にしてください。
+ # - **API サーバ**: GCP VM `http://34.146.231.219:8000` で稼働中（2026-09 時点）。
+ #   演習環境（JupyterHub）では `FX_API_BASE_URL`（内部 IP）が自動設定されます。
```

`document/tutorial_answer.py:1015`

```diff
- # - **`userId`**: `TraderFinder` で DB を引くため、**未登録のIDではセッションを開始できません**。
+ # - **`userId`**: サーバ側の許可リストにより、**未登録のIDではセッションを開始できません（403）**。
```

### #25 「上から順に実行」の範囲を明示

`document/colab_template.py:160`

```diff
- # 以下のコード群を上から順に実行することで、セッション開始に必要な事前準備を行うことができます。
+ # この下のセルを、上から順に「解答例」セルの手前まですべて実行すると、セッション開始に必要な事前準備が整います。
```

### #15 完走時間の予告

`document/colab_template.py:481`

```diff
- # 終了まで実行したい場合に使用する
+ # 終了まで実行したい場合に使用する（1 シナリオ 1,439 tick ≒ 20〜30 秒。進捗バーが出ます）
```

## 対応済み（参考）

- **app サーバ負荷**: 1 人の全力実行で 1 コアの 75% を消費（e2-medium では同時 2〜3 人が限界）。当日スケールアップ手順を運営手順 §5-5 に記載済み
- **Day 2 教材（colab_template）の動かない問題一式**: PR #25 で修正済み（マージ待ち）
- **参加者 ID**: 再生成・Hub / traders 登録・旧 ID 削除まで完了
