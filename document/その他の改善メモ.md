# その他の改善メモ（細目）

最終更新: 2026-09-11。大きな改善点は `document/来年への改善点.md` にまとめている。ここには文言修正・UX・リハーサル時の所見など細かいものを残す。

## A. 未対応の TODO（旧 document/tmp/todo_backlog.md より）

### A-1. Web UI で提出戦略のパフォーマンスを見る
- 参加者/運営が、提出済みセッションの成績（最終 JPY・リターン・推移）をブラウザで確認できる画面
- 既存 API で材料は揃っている: `GET /api/trade/sessions/{user_id}`、`GET /api/trade/session/{session_id}/history`
- 現状は fxapp.netlify.app に個人単位のダッシュボードがある。運営用のリーダーボード（EVAL 5 本の中央値と順位）は無い

### A-2. 負荷テストの定常化
- 実測（2026-09-06）: e2-medium/workers2 は同時 3〜5 人が限界、e2-standard-8/workers8 は並列 10 まで単発同等、20 で DB CPU が律速。手順は運営手順 §5-5
- 全シナリオ実行スクリプト `scripts/verify_all_scenarios.py` でも 4 並列で約 50 tick/s に頭打ちだった

## B. リハーサル所見（2026-09-02〜03、旧 document/tmp/rehearsal_findings_memo.md より）

対応状況は 2026-09-03 時点の適用トラッカーと、その後の作業を反映。

| # | 内容 | 状況 |
|---|---|---|
| 1 | パスワード忘れは `/opt/tljh/state/passwords.dbm` の該当キー削除で、ユーザーを消さずに再設定できる | reset-password スキルとして実装済み |
| 2 | 初回ログイン画面が「Sign in」で PW 新規設定と伝わらない → `announcement_login` で案内 | 参加者向け手順に記載。Hub 側の announcement は未設定 |
| 3 | tutorial の `testuser` デフォルト値で採点が汚れる → デフォルトのままなら raise | 対応済み（USER_ID='dummy' + ValueError 方式） |
| 4 | 「生成 AI・検索の利用は自由」を明文化 | FAQ に記載あり。ガイド側は要確認 |
| 5 | HTTP だと Chrome から初回ログインできない → HTTPS 化 | 対応済み（sslip.io + Let's Encrypt） |
| 6 | Day1 資料の `localhost:8000` 前提を env 方式に | 対応済み |
| 7 | 初期資産 100 万→1 億は技術的に可。金額記載の一括変更と合格ライン再計算が必要 | 未決（来年への改善点 #16） |
| 8 | API とリポジトリから EVAL レートが事前に見える | 一覧非表示・EVAL 期間の 403 は対応済み。リポジトリ側は未対応（来年への改善点 #1, #2）。変更系 API の無認証も未対応 |
| 9 | matplotlib の日本語フォントはカーネル再起動が必要 → TLJH 側に事前導入 | 回避策（japanize-matplotlib）を教材で案内。事前導入は未実施 |
| 10 | tutorial のコード内コメント日本語化 | 未対応（量が多い） |
| 11 | 評価シナリオ名は開発時間の終盤に発表する運用 | 今回そのとおり運用（14:00 頃発表、14:40 締切） |
| 12 | TEST シナリオが 2026-06 に固まりすぎ → 年・局面を散らす | 対応済み（TEST0〜18、2019〜2026 年） |
| 13 | Day1 の提出で「謎の URL」が出る → requests の HTTPError の traceback。日本語メッセージに変換 | day2 は #54 で detail 表示に。day1 は未対応 |
| 14〜17, 20, 22〜25 | colab_template（現 day2）の文言・UX 修正各種 | PR #28 以降で対応済み |
| 18 | 「DB に登録済みの ID」という実装詳細を参加者に見せない | 対応済み |
| 19 | FAQ の新設・拡充 | 一部あり。項目の中身は未決 |
| 21 | 実行時に start の URL が echo される／findfont 警告 | ①は出所未特定、②は項目 9 と同根 |
| 26 | tutorial_answer にローカル API 前提の残骸 | 対応済み |

## C. 運用上の細かい気づき（2026-09-08〜11）

- `11_distribute_to_homes.sh` の最終行で「GLOB: unbound variable」が出る（全角括弧と変数展開の組み合わせ）。処理は完了後なので実害なし。`${GLOB}` にすれば消える
- VM 起動直後は SSH が拒否される。`gcloud compute ssh ... --command=true` でリトライしてから compose up
- zsh では `P="--project=... --zone=..."` のような変数展開が単語分割されず gcloud が失敗する。フラグは直書きする
- `data/traders.csv` は許可リストと同じ 85 件（参加者 60 + group 10 + tester 8 + 運用 7）に揃えた。type 列は未使用のため削除済み
- 旧デモ由来の CNY レート（393 行）が EVAL2 の先頭 tick に混入していた。DB から削除済み。rates.csv に CNY は無い
- TEST16/TEST18（2025-11-04、11-06）の 17:01 に TRY/MXN/NOK/SEK/HKD の欠損があった。forward-fill で補完済み（#61）
- PoC 用の Netlify サイト（fx-trading-simulator-webui.netlify.app）は 2025-06 のビルドのまま動かない。本番は fxapp.netlify.app。WebUI リポジトリの README に PoC の URL が「本番」として残っている
