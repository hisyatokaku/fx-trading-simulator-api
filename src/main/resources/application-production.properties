# Production Environment Configuration
server.port=8080
feature.dbtype=PSQL

# Production Database Configuration (Sakura)
db.hostUrl=os3-389-27987.vs.sakura.ne.jp
db.port=5432
db.user=rocky
db.password=rocky2449
db.schema=fxtrade

# Spring DataSource Configuration
spring.datasource.url=jdbc:postgresql://${db.hostUrl}:${db.port}/${db.schema}
spring.datasource.username=${db.user}
spring.datasource.password=${db.password}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Game Configuration
gameconfig.initialJpyAmount=1000000

# Production specific settings
logging.level.com.example.fxtrade=INFO
logging.level.org.springframework=WARN

# CORS Configuration for Production
cors.allowed-origins[0]=https://your-production-frontend.com
cors.allowed-origins[1]=https://yourdomain.com
cors.allowed-methods[0]=GET
cors.allowed-methods[1]=POST
cors.allowed-methods[2]=PUT
cors.allowed-methods[3]=DELETE
cors.allowed-methods[4]=OPTIONS
cors.allowed-headers[0]=Content-Type
cors.allowed-headers[1]=Authorization
cors.allowed-headers[2]=X-Requested-With
cors.allow-credentials=false
cors.max-age=3600